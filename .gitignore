# Deploy ONLY the public website. Everything else here (audit/proposal PDFs & HTML,
# raw source photos, the build/ generator source) stays PRIVATE.
/*
!/.gitignore
!/.nojekyll
!/.github/
!/index.html
!/images/
!/assets/
!/services/
!/dumpster-sizes/
!/service-areas/
!/blog/
!/about/
!/contact/
!/pricing/
!/faq/
!/sitemap.xml
!/robots.txt
!/404.html

# Proposal-only asset (a screenshot of the homepage), not needed on the live site:
/images/new-website-preview.png
